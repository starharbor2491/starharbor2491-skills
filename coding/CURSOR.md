# Cursor

@AGENTS.md
@../shared/base.md

If this folder sits in a project (or you copied these files to a project root), treat `AGENTS.md` as the rule file, apply the smallest patch that compiles, and keep the composer from rewriting neighboring files that were not in the request.
