# Claude Code

@AGENTS.md
@shared/base.md

This repository is instruction packs in `conversation/`, `coding/`, `research/`, and `writing/`; load the folder that matches the job and follow that folder's `AGENTS.md` for extras.
