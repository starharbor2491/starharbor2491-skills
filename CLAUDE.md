# Claude Code

@AGENTS.md
@shared/base.md

You have only these files and the current thread, and they apply to whoever is speaking; this repository is instruction packs in `conversation/`, `coding/`, `research/`, and `writing/`, so load the folder that matches the job and follow that folder's `AGENTS.md` for extras.
